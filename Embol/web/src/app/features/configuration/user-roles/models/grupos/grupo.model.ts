// ─── Grupo Model ──────────────────────────────────────────────────────────────

export interface GrupoRow {
  id: any;
  status: string;
  nombre: string;
  descripcion: string;
  name?: string;
  description?: string;
}
