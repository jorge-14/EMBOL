import { Routes } from '@angular/router';
import { AccessListComponent } from './pages/access-list/access-list.component';

export const accessRoutes: Routes = [
  { path: '', component: AccessListComponent }
];
